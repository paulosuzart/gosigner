Sample app to run o GAE golang runtime.

Just playing with backbone.js and underscore.js templates.
The app runs without any Go template. That is, htmls are server directly by GAE runtime and backbone.js is responsible to make things work with the server.
